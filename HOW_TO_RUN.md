# كيفية تشغيل واختبار نظام ERP | How to Run & Test the System

## الخطوة 1: التشغيل البسيط الفوري ⚡

### بدء التطبيق:
```bash
# استنسخ المستودع
git clone https://github.com/amido57/comprehensive-erp-system.git
cd comprehensive-erp-system

# شغل Docker (الخيار الأسهل)
docker-compose up -d

# انتظر 60 ثانية

# افتح المتصفح
http://localhost
```

### بيانات الدخول:
```
Email: admin@erp.test
Password: password
```

---

## الخطوة 2: اختبار الواجهة الرئيسية 📄

بعد تسجيل الدخول، ستشاهد:

### 1. لوحة التحكم (Dashboard)
- معلومات ملخصة عن العمل
- إجمالي المبيعات 
- أحدث الطلبات
- حالة المخزون

### 2. القائمة الجانبية
- المبيعات 📊
- المخزون 📦
- الحسابات 💰
- الموارد البشرية 👥
- التقارير 📈
- الإعدادات ⚙️

---

## الخطوة 3: اختبار كل ميزة ✨

### أ. إدارة المنتجات (Products)

```
1. اذهب إلى القائمة الرئيسية
2. اختر "المخزون" → "المنتجات"
3. اضغط "إضافة منتج جديد"
4. أكمل البيانات:
   - اسم المنتج
   - السعر
   - الكمية المتاحة
   - الفئة
5. احفظ المنتج
```

### ب. إنشاء فاتورة (Create Invoice)

```
1. اذهب إلى "المبيعات" → "الفواتير"
2. اضغط "فاتورة جديدة"
3. اختر العميل (أو أنشئ واحد جديد)
4. أضف المنتجات:
   - اختر المنتج
   - أدخل الكمية
   - سيتم حساب السعر تلقائياً
5. أضف ملاحظات إن وجدت
6. احفظ الفاتورة
7. يمكنك طباعتها أو تصديرها
```

### ج. إدارة المخزون (Inventory)

```
1. اذهب إلى "المخزون"
2. شاهد قائمة المنتجات الكاملة
3. يمكنك:
   - عرض الكمية المتاحة
   - تحديث الأسعار
   - تتبع المنتجات المنخفضة
   - إضافة تنبيهات عند نقص المخزون
```

### د. التقارير (Reports)

```
1. اذهب إلى "التقارير"
2. اختر نوع التقرير:
   - تقرير المبيعات
   - تقرير المخزون
   - تقرير الأرباح والخسائر
   - تقرير الفائض
3. حدد الفترة الزمنية
4. انقر "إنشاء تقرير"
5. صدّر كـ PDF أو Excel
```

### ه. الحسابات (Accounting)

```
1. اذهب إلى "الحسابات"
2. يمكنك:
   - عرض جميع المعاملات
   - إنشاء قيود يدوية
   - عرض الميزانية العمومية
   - تقرير الدخل
   - الضرائب والرسوم
```

### و. الموارد البشرية (HR)

```
1. اذهب إلى "HR"
2. إدارة الموظفين:
   - إضافة موظف جديد
   - تحديث البيانات
   - تتبع الرواتب
   - الإجازات والحضور
```

---

## الخطوة 4: اختبار API 🔐

### الحصول على توكن (Get Token)

```bash
curl -X POST http://localhost:8000/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@erp.test",
    "password": "password"
  }'

# ستحصل على:
{
  "token": "eyJ0eXAiOiJKV1QiLC...",
  "user": { ... }
}
```

### اختبار API Endpoints

```bash
# احصل على قائمة المنتجات
curl -X GET http://localhost:8000/api/products \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Accept: application/json"

# احصل على قائمة الفواتير
curl -X GET http://localhost:8000/api/invoices \
  -H "Authorization: Bearer YOUR_TOKEN"

# احصل على تقرير المبيعات
curl -X GET http://localhost:8000/api/reports/sales \
  -H "Authorization: Bearer YOUR_TOKEN"

# أنشئ منتج جديد
curl -X POST http://localhost:8000/api/products \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "منتج جديد",
    "price": 100,
    "quantity": 50
  }'
```

---

## الخطوة 5: الميزات المتقدمة 🚀

### تعدد اللغات
- انقر على أيقونة اللغة في أعلى الصفحة
- اختر بين العربية والإنجليزية
- جميع البيانات تتحدث باللغة المختارة

### الأدوار والأذونات
- في الإعدادات، يمكنك إضافة مستخدمين
- تعيين أدوار مختلفة (مدير، محاسب، بائع، إلخ)
- كل دور له صلاحيات محددة

### التصدير والطباعة
- أي تقرير أو فاتورة يمكن:
  - طباعتها مباشرة
  - تصديرها كـ PDF
  - تصديرها كـ Excel

### الإشعارات
- تنبيهات عند نقص المخزون
- تنبيهات عند تجاوز الفواتير المستحقة
- تنبيهات عند وصول رواتب الموظفين

---

## استكشاف الأخطاء 🔧

### المشكلة: لا يمكن الاتصال
```bash
# تحقق من حالة Docker
docker-compose ps

# عرض السجلات
docker-compose logs -f

# أعد التشغيل
docker-compose restart
```

### المشكلة: المنفذ مشغول
```bash
# أيقف جميع الحاويات
docker-compose down

# نظّف النظام
docker system prune -a

# شغّل مرة أخرى
docker-compose up -d
```

### المشكلة: لا يمكن تسجيل الدخول
```bash
# أعد تعيين كلمة المرور
docker-compose exec app php artisan tinker

# في الـ Tinker:
App\\Models\\User::first()->update(['password' => bcrypt('password')]);
exit;
```

---

## نصائح مهمة 📚

✅ استخدم **Chrome أو Firefox** للحصول على أفضل تجربة
✅ تأكد من أن **Docker قيد التشغيل**
✅ استخدم **localhost** بدلاً من 127.0.0.1
✅ امسح **ذاكرة التخزين المؤقت** إذا واجهت مشاكل
✅ استخدم **الوضع الداكن** (Dark Mode) إذا أردت
✅ تحقق من **السجلات** عند حدوث خطأ

---

## المزيد من الموارد 📚

- [RUN_LOCALLY.md](RUN_LOCALLY.md) - دليل التشغيل السريع
- [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md) - دليل النشر
- [BACKEND_SETUP.md](BACKEND_SETUP.md) - توثيق API
- [ARCHITECTURE.md](ARCHITECTURE.md) - بنية النظام

---

## الدعم والمساعدة 💬

- 📧 لأي مشاكل، افتح [GitHub Issue](https://github.com/amido57/comprehensive-erp-system/issues)
- 💡 للاقتراحات، استخدم [GitHub Discussions](https://github.com/amido57/comprehensive-erp-system/discussions)
- 🌐 للترجمات، ساهم في المشروع

---

**الآن أنت جاهز! ابدأ الاستكشاف والتطوير! 🎉**
